// ============================================================
// ThemeWizardPage.qml — macOS 27 风格安装向导页面
// 注：此页面保留用于兼容，新版在独立窗口 ThemeWizardWindow.qml 中
// 因此页面不再从 StackLayout 引用，可安全忽略
// ============================================================
import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import Backend 1.0

Rectangle {
    id: root
    color: "transparent"
    visible: false
}
